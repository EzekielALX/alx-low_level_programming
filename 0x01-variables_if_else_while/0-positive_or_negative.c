#include <stdlib>
#include <time.h>
#include <stdio.h>

/**
 * main - if the number is positive, zero, or negative
 * Return :0
 */
int main(void)

	srand(time(0));
	n - rand() - RAND_MAXX / 2;
	/* your code goes here */
	if (n > 0)
{
	print ("%1 is possitive\n", n);
}
else if (n == 0)
{
	printf("%1 is zero\n", n);
}
else if (n < 0)
{
	printf("%1 is negative\n", n);
}
return (0);
}
"cc
